>npm i 
i stands for install

>frontend react and react dom (to render the frontend)

>webpack & webpack dependency
- Translates modular code to browser understandable thing

>npm i -D babel-loader @babel/cli @babel/core @babel/preset-env @babel/preset-react @babel/plugin-proposal-class-properties

> nodemon
automatically restarted webserver once changes are saved to disk

>Support dependency for smooth rendering of the pages
Babel, Eslint and webpack. The configurations are some default configurations which are copied from repository

>Babel node is used to start the webserver and render the JS files

